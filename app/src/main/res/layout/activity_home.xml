<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <include
        android:id="@+id/actionbar_home_activity"
        layout="@layout/actionbar_home" />

    <Button
        android:id="@+id/toWhereaboutsSceenButton"
        android:layout_width="match_parent"
        android:layout_height="100dp"
        android:layout_marginHorizontal="30dp"
        android:layout_marginTop="30dp"
        android:background="@color/yellow"
        android:shadowColor="#0E0E0E"
        android:text="@string/button_whereabouts"
        android:textColor="@color/white"
        android:textSize="33dp"
        android:textStyle="bold"
        app:backgroundTint="@color/yellow"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/actionbar_home_activity" />

    <Button
        android:id="@+id/toRemindersScreenButton"
        android:layout_width="match_parent"
        android:layout_height="100dp"
        android:layout_marginHorizontal="30dp"
        android:layout_marginTop="30dp"
        android:background="@color/yellow"
        android:shadowColor="#0E0E0E"
        android:text="@string/button_reminders"
        android:textColor="@color/white"
        android:textSize="33dp"
        android:textStyle="bold"
        app:backgroundTint="@color/yellow"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/toWhereaboutsSceenButton" />

</androidx.constraintlayout.widget.ConstraintLayout>